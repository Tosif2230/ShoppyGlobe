import React from 'react'

function Checkout() {
  return (
    <div>
      CheckOut Page

      <div>Checkout: Create a dummy form to collect user details, along with a summary of the products added to the cart. Include a "Place Order" button. Upon clicking the button, display a message saying "Order placed" and empty the cart, then redirect the user back to the Home page automatically.</div>
    </div>
  )
}

export default Checkout
